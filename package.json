{
  "name": "runtime-config-monorepo",
  "private": true,
  "version": "0.1.2",
  "description": "Framework-agnostic runtime config — inspired by Nuxt 3/4 useRuntimeConfig",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun --filter='*' run build",
    "dev": "bun --filter='*' run dev",
    "clean": "bun --filter='*' run clean",
    "pub": "bun run build && (cd packages/core && npm publish) && (cd packages/vite && npm publish) && (cd packages/react && npm publish)",
    "pub:dry": "bun run build && (cd packages/core && npm publish --dry-run) && (cd packages/vite && npm publish --dry-run) && (cd packages/react && npm publish --dry-run)",
    "release": "changelogen --release --bump",
    "release-minor": "changelogen --release --minor",
    "release-patch": "changelogen --release --patch"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "changelogen": "^0.6.2",
    "typescript": "^6.0.3"
  }
}
